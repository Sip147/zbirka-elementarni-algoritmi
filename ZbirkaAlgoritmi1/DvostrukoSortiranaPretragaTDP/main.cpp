#include <bits/stdc++.h>

using namespace std;

int n,m;

int main(){

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    cin>>m>>n;
    vector<vector<int>>matrica;
    for(int i=0;i<m;i++){
        vector<int>red(n);
        for(int j=0;j<n;j++){
            cin>>red[j];
        }
        matrica.push_back(red);
    }

    int x;
    while(cin>>x){
        int i1=0,j1=0,i2=0,j2=0;


    }

}
